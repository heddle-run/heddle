{
  "name": "@heddle/broker",
  "version": "0.2.0-beta.1",
  "private": true,
  "description": "Cloudflare Worker fronting the heddle playground engine",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.73.0"
  }
}

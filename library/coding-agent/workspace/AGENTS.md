# Working in this project

- Run the tests with `python3 -m unittest discover -s tests -v`. There is no
  other test runner and nothing to install.
- Keep changes minimal. Fix what the task names; do not reformat or rename
  anything else.
- Every function keeps its docstring accurate. If a fix changes behaviour,
  the docstring moves with it.

{
  "weave": 1,
  "name": "ag-ui-demo",
  "description": "A minimal flow, so a rendering has a lifecycle to render. The switch needs no model and no tool: the run is inputs, one step, one outcome.",
  "inputs": {
    "query": "string"
  },
  "steps": [
    {
      "name": "inspect",
      "switch": "{{inputs.query}}",
      "else": "done"
    }
  ],
  "outcomes": {
    "done": {
      "query": "{{inputs.query}}"
    }
  }
}

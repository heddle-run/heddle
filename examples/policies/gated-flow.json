{
  "weave": 1,
  "name": "gated-demo",
  "description": "An agent that asks for a shell command the operator does not allow. The model is a stub provider from the policies plugin, so this runs with no credential.",
  "inputs": {
    "task": "string"
  },
  "tools": {
    "shell": {
      "description": "Run a shell command.",
      "inputs": { "cmd": "string" },
      "outputs": { "stdout": "string" }
    }
  },
  "requires": {
    "StubModelConfig": "^1.0"
  },
  "steps": [
    {
      "name": "agent",
      "agent": {
        "model": { "provider": "StubModelConfig", "model": "stub" },
        "prompt": "Do what the task asks.\n\nThe task: {{inputs.task}}",
        "tools": ["shell"]
      }
    }
  ]
}
